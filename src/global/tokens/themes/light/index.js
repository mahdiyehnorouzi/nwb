const { boxShadow } = require("./boxShadow");
const { backgroundColor } = require("./background");
const { borderColor } = require("./border");
const { textColor } = require("./text");
const { ringColor } = require("./ringColor");

exports.lightTheme = {
  backgroundColor,
  textColor,
  borderColor,
  boxShadow,
  ringColor,
};
